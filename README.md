# Документация Проекта "Я и Деньги"

## 1. Архитектура и Структура
Проект построен по модульному принципу с четким разделением ответственности. 

### 1.1. Backend (Node.js)
Основная логика сервера сосредоточена в `server/models/GameState.js`, который работает как **Фасад (Facade)**. Он делегирует задачи специализированным менеджерам:
*   **PlayerManager**: Авторизация игроков и куратора, управление списком участников.
*   **TurnManager**: Контроль очередности ходов, таймеры, броски кубика.
*   **MovementManager**: Расчет путей перемещения, логика развилок (форок) и монетки.
*   **FinanceManager**: Сложная финансовая логика: кошельки, пассивный доход, покупка активов.
*   **CellManager**: Обработка всех эффектов клеток поля и карт.

### 1.2. Frontend (Vanilla JS)
Клиентская часть синхронизируется с сервером через Socket.IO.
*   **gameclient.js**: Основной контроллер, управляющий очередью событий (**Event Queue**) для предотвращения наложения окон.
*   **board.js**: Логика интерфейса, модальных окон и финансовой карточки.
*   **Анимации**: Вынесены в отдельные модули (`diceanimation.js`, `playeranimation.js` и т.д.).

---

## 2. Игровой Процесс

### 2.1. Авторизация и Вход
1.  **Игрок**: Вводит имя и пароль игры (по умолчанию `game`). Получает уникальный ID (UUID) и цвет фишки.
2.  **Куратор**: Входит под паролем `curator`. Имеет доступ к управлению игрой и полной статистике.

### 2.2. Ход и Перемещение
1.  **Бросок**: Активный игрок нажимает на кубик. Сервер считает результат и уведомляет всех.
2.  **Движение**: Фишка плавно перемещается. Если на пути есть клетки "Деньги", они фиксируются для выплаты дохода.
3.  **Развилки (Fork)**: При попадании на развилку активируется монетка. Результат (Орел/Решка) определяет направление следующего движения.

### 2.3. Финансы и Кошельки
Игра использует систему из 4-х кошельков (копилок):
*   ❤️ **Благотворительность**: Пожертвования и бонусы к кубику.
*   ⭐ **Мечта**: Накопления на главную цель.
*   🛡️ **Сбережения**: Резервный фонд.
*   📈 **Инвестиции**: Средства для покупки бизнесов.

---

## 3. Технические Особенности
*   **Socket.IO**: Обеспечивает мгновенное обновление состояния у всех игроков.
*   **Единая очередь событий**: Все важные сообщения и карты показываются по очереди, чтобы игрок ничего не пропустил.
*   **JSON-логика**: Описания и эффекты клеток вынесены в `data/cells.json`, что позволяет легко менять баланс игры без изменения кода.
*   **Синхронизация**: Игроки могут вести «бумажный» учет в финансовой карточке, но сервер всегда хранит эталонные («фактические») данные, которые можно синхронизировать одной кнопкой.
*   **Game Over**: При завершении игры отображается модальное окно с информацией о победителях и анимация салюта (confetti).
*   **Управление куратора**: Куратор может скрывать/показывать кнопку управления игрой у всех игроков для полного контроля над процессом.

---

## 4. Развертывание
1.  Установите зависимости: `npm install`
2.  Настройте `.env` (пароли игры и куратора).
3.  Запустите сервер: `npm start`
4.  Игра доступна по адресу сервера (порт по умолчанию 3000).
