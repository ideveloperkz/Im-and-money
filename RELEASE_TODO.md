# RELEASE TODO (Напоминание перед релизом)

Этот файл содержит список технических правок и "костылей", которые должны быть удалены перед финальным релизом.

## Критически важно
- [ ] **Удалить технические номера карточек (NR)** из отображения.
  - **Файл:** `server/services/CardService.js` (был в `GameState.js`)
  - **Место:** Метод `processCard`, где к `card.message` или заголовку добавляется `[№${card.nr}]`.
  - **Зачем:** Игроки не должны видеть внутреннюю нумерацию карт.

## Проверка баланса
- [ ] Проверить значения в `server/data/cells.json` (цены штрафов, доходность).
- [ ] Убедиться, что пароли в `.env` на продакшене отличаются от дефолтных (`game`/`curator`).

## Оптимизация
- [ ] Отключить лишние `console.log` в `gameclient.js` и `server.js` (оставить только важные события).
